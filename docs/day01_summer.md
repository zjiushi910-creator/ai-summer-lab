# Day 1 / 56 Summary

## Today I learned

- How to create a PyCharm project
- How to create a virtual environment `.venv`
- How to create project folders: `src`, `docs`, `data`, `configs`, `tests`, `notebooks`
- How to create `README.md`, `.gitignore`, and `requirements.txt`
- How to install packages with `pip install -r requirements.txt`
- How to use `logging.info()`
- How to use `numpy.ndarray`
- How to create a simple vector with `np.array`
- How to check vector shape with `vector.shape`

## Important concepts

### 1. requirements.txt

`requirements.txt` records the Python packages required by this project.

Example:

```txt
numpy
pandas
matplotlib
tqdm
```

```bash
pip install -r requirements.txt
```